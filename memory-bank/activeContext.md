# Active Context

Current task: None

Mode: Ready for next task

State:
- Previous task `open-brain-vm-supabase-setup` is completed and archived.
- Ready to begin a new task.

Important context:
- Deployed MCP endpoint: `https://dxgppqznnwxwavvuwhza.supabase.co/functions/v1/open-brain-mcp`
- Do not store connector URLs with live access keys in repo files.
- The local VM-side access key file used for testing should remain private to the VM.

Next action:
- Start the next task with VAN mode or a fresh task definition.
